{
  "name": "customer-dashboard",
  "schema": "customer",
  "views": [
    {"type": "summary", "field": "balance", "operation": "sum"},
    {"type": "table", "columns": ["customer_id", "name", "balance"]}
  ]
}
